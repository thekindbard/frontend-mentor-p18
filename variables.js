
const CAR = document.querySelector('.car');
const CAR_INFO = document.querySelectorAll('.car-info');
const CARRUCEL_CLOSE = document.querySelector('.gallery-close');
const GALLERY = document.querySelector('.gallery');

const UP = document.querySelectorAll('.gallery-arrows .up ');
const DOWN = document.querySelectorAll('.gallery-arrows .down');

var  ACT_IMG = 1;
const MAX_IMG = 4;
const MIN_IMG = 1;

export {
          CAR, 
          CAR_INFO, 
          CARRUCEL_CLOSE, 
          GALLERY, 
          UP,
          DOWN,
          ACT_IMG, 
          MAX_IMG,
          MIN_IMG,
};